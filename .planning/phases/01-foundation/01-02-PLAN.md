---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - railway.toml
  - .env.example
autonomous: false

user_setup:
  - service: railway
    why: "24/7 bot hosting"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> Bot -> Token (Reset Token button)"
    dashboard_config:
      - task: "Enable Server Members Intent"
        location: "Discord Developer Portal -> Applications -> Bot -> Privileged Gateway Intents -> SERVER MEMBERS INTENT"
      - task: "Enable Message Content Intent"
        location: "Discord Developer Portal -> Applications -> Bot -> Privileged Gateway Intents -> MESSAGE CONTENT INTENT"

must_haves:
  truths:
    - "Guardian bot is online 24/7 on Railway"
    - "#verify channel exists in Discord server"
    - "#security-logs channel exists in Discord server"
    - "@Unverified role exists in Discord server"
    - "@Verified role exists in Discord server"
    - "Bot assigns @Unverified role when test member joins"
  artifacts:
    - path: "railway.toml"
      provides: "Railway deployment configuration"
      contains: "start"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "DISCORD_BOT_TOKEN"
  key_links:
    - from: "Railway service"
      to: "src/guardian/guardian.py"
      via: "start command executes python -m"
      pattern: "python -m guardian.guardian"
    - from: "src/guardian/guardian.py"
      to: "Discord Gateway"
      via: "client.run() WebSocket connection"
      pattern: "client\\.run.*DISCORD_BOT_TOKEN"
---

<objective>
Deploy Guardian to Railway for 24/7 uptime and verify infrastructure creation.

Purpose: Transition Guardian from local development to production hosting, confirm infrastructure auto-creates correctly in live Discord server.
Output: Guardian bot running on Railway with verified channel/role creation and member join handling.
</objective>

<execution_context>
@/Users/lexchristopherson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lexchristopherson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lexchristopherson/Developer/mcp/discord-mcp/.planning/PROJECT.md
@/Users/lexchristopherson/Developer/mcp/discord-mcp/.planning/ROADMAP.md
@/Users/lexchristopherson/Developer/mcp/discord-mcp/.planning/STATE.md
@/Users/lexchristopherson/Developer/mcp/discord-mcp/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/lexchristopherson/Developer/mcp/discord-mcp/.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Railway deployment configuration</name>
  <files>
    railway.toml
    .env.example
  </files>
  <action>
Create railway.toml in project root with worker configuration:

```toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m guardian.guardian"
healthcheckPath = ""
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
```

Why no healthcheckPath: Guardian is a worker process (Gateway WebSocket), not an HTTP server. Railway will monitor process status instead of HTTP endpoint.

Create .env.example showing required environment variables:
```
DISCORD_BOT_TOKEN=your_bot_token_here
```

Do NOT commit actual .env file with token. Add .env to .gitignore if not already present.
  </action>
  <verify>
cat railway.toml shows startCommand with python -m guardian.guardian
cat .env.example shows DISCORD_BOT_TOKEN placeholder
grep "\.env$" .gitignore confirms .env is gitignored (or add if missing)
  </verify>
  <done>
railway.toml configured for worker deployment with guardian.guardian entry point, .env.example documents required variables
  </done>
</task>

<task type="auto">
  <name>Deploy Guardian to Railway</name>
  <files></files>
  <action>
Deploy Guardian bot to Railway using Railway CLI.

Steps:
1. Login to Railway: railway login
2. Create new project: railway init --name discord-guardian
3. Link to current directory: railway link
4. Set environment variable: railway variables --set DISCORD_BOT_TOKEN=<token_from_user_setup>
5. Deploy: railway up
6. Monitor logs: railway logs (verify bot connects and creates infrastructure)

Expected log output:
- "Logged in as Guardian#1234"
- "Created Unverified role in [Server Name]" (or "Found existing Unverified role")
- "Created Verified role in [Server Name]"
- "Created #verify channel in [Server Name]"
- "Created #security-logs channel in [Server Name]"
- "Guardian ready in 1 guilds"

If authentication errors occur, pause and create checkpoint for user to complete Railway login.

Railway will auto-detect Python project, install dependencies from pyproject.toml, and execute startCommand from railway.toml.
  </action>
  <verify>
railway status shows service running
railway logs shows "Guardian ready in" success message
Discord server shows bot online (green status)
  </verify>
  <done>
Guardian deployed to Railway, logs confirm successful infrastructure creation, bot shows online in Discord
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Guardian bot deployed to Railway with auto-provisioned infrastructure (#verify, #security-logs channels, @Unverified/@Verified roles) and member join handling.
  </what-built>
  <how-to-verify>
**Verify infrastructure creation:**
1. Open Discord server where Guardian is installed
2. Check channels list for #verify and #security-logs channels
3. Check roles list for @Unverified and @Verified roles
4. Verify #verify channel permissions:
   - @everyone should NOT see this channel
   - Click #verify channel settings -> Permissions -> @everyone should show "❌ View Channel"
   - @Unverified should be able to see it (green checkmark)

**Verify member join handling:**
5. Create a test Discord account or use alt account
6. Join the Discord server with test account
7. Confirm test account is immediately assigned @Unverified role
8. Check #security-logs for member join event (optional, depends on logging implementation)
9. Remove test account from server when done

**Verify 24/7 uptime:**
10. Run: railway logs --tail 50
11. Confirm no errors, no disconnections, logs show stable connection
12. Check Railway dashboard shows service status: "Running"

Expected state: Bot online, 4 infrastructure pieces exist (#verify, #security-logs, @Unverified, @Verified), test member gets @Unverified role on join.
  </how-to-verify>
  <resume-signal>
Type "approved" if all verification steps pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
1. railway.toml exists with guardian.guardian start command
2. Railway service deployed and running
3. Bot online in Discord (green status indicator)
4. #verify channel exists with correct permissions (@everyone denied)
5. #security-logs channel exists
6. @Unverified and @Verified roles exist
7. New members automatically receive @Unverified role
8. Railway logs show stable connection without errors
</verification>

<success_criteria>
- Guardian maintains 24/7 uptime on Railway
- Bot auto-creates all 4 infrastructure components (#verify, #security-logs, @Unverified, @Verified)
- Infrastructure creation is idempotent (restart doesn't create duplicates)
- New members receive @Unverified role immediately on join
- #verify channel isolated to @Unverified role only
- Railway logs confirm successful Gateway connection
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
